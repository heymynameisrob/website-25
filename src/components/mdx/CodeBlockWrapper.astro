---
import { CodeBlock } from "@/components/CodeBlock";

const { "data-file-name": dataFileName, ...rest } = Astro.props;
---

<div class="my-12 not-prose text-sm">
  <CodeBlock client:load fileName={dataFileName} {...rest}><slot /></CodeBlock>
</div>
